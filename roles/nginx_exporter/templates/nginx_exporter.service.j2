[Unit]
Description=NGINX Prometheus Exporter for {{ item.name }}
After=network.target

[Service]
User=root
ExecStart=/usr/local/bin/nginx-prometheus-exporter --nginx.scrape-uri={{ item.scrape_uri }} --web.listen-address=:{{ item.port }}
Restart=always

[Install]
WantedBy=multi-user.target
